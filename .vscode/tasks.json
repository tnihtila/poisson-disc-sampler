{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "Build poisson-disc-sampler project",
			"command": "mkdir -p build && cd build && cmake .. && make -j 16",
			"group": "build"
		}
	]
}